enum TransactionStatus {
  pending,
  approved,
  rejected,
}

enum TransactionType {
  debit,
  credit,
}

enum TransactionCategory {
  groceries,
  dining,
  transport,
  shopping,
  utilities,
  entertainment,
  health,
  transfer,
  income,
  other,
}
